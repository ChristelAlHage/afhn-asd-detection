# AFHN — Python dependencies
# Tested on Google Colab T4 (May 2026).
# The notebook installs these automatically; this file is for local/CI reproduction.

# --- Core ML stack ---
tensorflow==2.20.0
numpy>=2.0.0,<2.1.0   # 2.0+ required (the notebook uses np.trapezoid)
scikit-learn>=1.4.0
pandas>=2.0.0
scipy>=1.13.0

# --- Image processing ---
opencv-python>=4.10.0

# --- Face detection ---
mtcnn==0.1.1          # Pin: newer versions changed API
dlib>=19.24.0         # Optional fallback landmark detector

# --- Facial landmarks (region-based XAI) ---
mediapipe>=0.10.18    # Uses the new Tasks API

# --- Explainability ---
shap>=0.46.0          # Uses GradientExplainer for RNN-containing models

# --- Demographic bias audit ---
deepface>=0.0.93      # For age/gender/race prediction

# --- Visualization ---
matplotlib>=3.7.0
seaborn>=0.13.0
tqdm>=4.66.0

# --- Jupyter (only needed if running locally) ---
jupyter>=1.0.0
ipykernel>=6.29.0
